schema_version: '1.0'
profile_meta:
  profile_id: s4_dense25_nsga2_raw_profile
  description: S4 dense25 tuning for NSGA-II raw variation, matching the active staged
    ladder.
family: genetic
backbone: nsga2
mode: raw
parameters:
  crossover:
    eta: 10
    prob: 0.9
  mutation:
    eta: 15
