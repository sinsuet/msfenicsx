schema_version: "1.0"
profile_meta:
  profile_id: s2_staged_nsga2_union_profile
  description: Active S2 staged tuning for NSGA-II union and llm variation. Parameters mirror the shared ladder so controller comparisons stay apples-to-apples.
family: genetic
backbone: nsga2
mode: union
parameters:
  crossover:
    eta: 10
    prob: 0.9
  mutation:
    eta: 15
